# Progress DannShop Topup Platform — Checkpoint

Terakhir update: 2026-08-06. **HANDOFF POINT — baca ini dulu sebelum apa pun lain di bawah (entri 2026-08-05 ke bawah sudah lama, konteksnya sudah diteruskan ke sini):**

**Status ringkas per area:**
1. **Batch pricing (urutan termurah, flash sale, grup item, fix bug `memberPrice` mati)** — kode selesai, **sudah di-push ke `origin/main`** (commit `5ba9d7f`). **BELUM diterapkan ke database production** — migrasi baru jalan di dev lokal. Wajib jalankan `npx dotenv -e .env.production -- npx prisma migrate deploy` (dari `web/`) sebelum/segera setelah deploy berikutnya, kalau belum sempat.
2. **Hover ring warna adaptif tema di kartu produk/tab kategori** — sempat dibuat (emerald terang/violet gelap), lalu user **membalikkannya sendiri ke ungu polos** di kedua tema setelah review visual (juga fix efek hover tab kategori yang "meluber"). Versi final (ungu-ungu, hover ringkas) sudah di-push (`dfc9574` lalu revert user `d78525c`) — **jangan diusulkan lagi diganti-ganti warnanya kecuali user minta.**
3. **Perluasan panel admin (sidebar terstruktur + 9 fitur baru: favicon, FAQ/TOS/Privasi editable, maintenance mode, laporan penjualan + isi dashboard, monitoring job, log webhook, markup harga massal)** — kode selesai, **sudah di-push** (commit `2d34908`). Nol migrasi schema (semua reuse model existing).
4. **Follow-up round (sesi yang sama, setelah #3): kontak WA/Telegram admin-editable di 4 tempat, FAQ ditulis ulang dari analisa fitur real, TOS/Privasi diperdalam (bukan review hukum sungguhan — lihat catatan di bawah), email config Resend+SMTP terenkripsi dari admin** — kode selesai, tsc/lint/176-test semua hijau, **BELUM di-commit/push saat checkpoint ini ditulis.** Cek `git status` di root repo — kalau masih ada perubahan di `web/src/lib/notify/email-config.ts`, `web/src/app/admin/settings/email-config-form.tsx`, dll., itu batch ini yang belum ke-push, bukan kerjaan lama yang lupa.

**Penting, jangan diulang keliru:** permintaan "review pengacara/badan hukum beneran" untuk TOS/Privasi **tidak bisa dieksekusi** — asisten bukan pengacara. Yang dikerjakan: draf jauh lebih lengkap (13 section TOS, rujukan UU PDP di Privasi) tapi teks di halamannya sendiri bilang eksplisit ini bukan pengganti konsultasi hukum sungguhan.

**Env var lama yang sekarang mati (jangan bingung kalau masih kelihatan di Vercel dashboard):** `RESEND_API_KEY`, `RESEND_FROM_EMAIL` — email sekarang dikonfigurasi lewat Admin > Pengaturan Situs > Pengiriman Email, tersimpan terenkripsi di DB (butuh `CREDENTIALS_ENCRYPTION_KEY` yang sudah ada, bukan var baru). `NEXT_PUBLIC_WHATSAPP_CS`/`NEXT_PUBLIC_TELEGRAM_CS` masih dipakai tapi cuma sebagai fallback sebelum admin isi lewat form baru.

**Insiden dependency yang perlu diketahui kalau ada `npm install` baru:** `nodemailer` yang ditambah buat SMTP config sempat bentrok peer-dependency sama versi lama yang sudah ditarik `next-auth` (versi lama itu ada CVE nyata). Di-fix dengan `"overrides": { "nodemailer": "^9.0.4" }` di `package.json` — jangan dihapus tanpa alasan, itu yang mencegah `npm install` gagal total.

Detail teknis lengkap semua poin di atas: memory `project_dannshop_repo.md` (bagian "Update 2026-08-06").

---

Terakhir update: 2026-08-05, sesi lanjutan (deadline user: **2026-08-05**, demo ke client besok 2026-08-06). **HANDOFF POINT — lanjut dari sini:** Bagian A, B, C **DAN** fitur logo metode pembayaran (upload admin + tampilan) + redesign search navbar + scrollbar disembunyikan sitewide — **SEMUA SELESAI, di-push ke `main`, dan dikonfirmasi live di production** (lihat bagian "Update 2026-08-05 (lanjutan)" di bawah untuk detail). **Satu-satunya yang masih menggantung dari sebelumnya: aktivasi Resend (email invoice)** — belum disentuh lagi di sesi lanjutan ini. Baca bagian "Status Bagian C" di bawah SEBELUM lanjut Resend — ada state penting (API key sudah didapat user tapi belum dikonfirmasi terpasang) yang harus diverifikasi dulu, jangan diulang dari nol.

**Update 2026-08-05 (lanjutan, sesi setelah handoff Resend) — logo metode pembayaran + redesign search navbar + scrollbar sitewide, semua selesai & live:** admin `/admin/payment-methods` sekarang bisa upload file logo (Vercel Blob) selain URL; login admin redirect ke `/admin`; logo di marquee (strip berjalan) dikoreksi 2x sesuai referensi user jadi plate putih rounded, logo di picker checkout/deposit dibenerin (sebelumnya kepotong lingkaran 20px karena logonya wordmark lebar); migration backfill `logoUrl` production yang tadinya NULL; search di navbar dipindah jadi tombol ikon di kanan (urutan: search, darkmode, sidebar) yang expand inline ke kiri saat diklik (bukan modal); scrollbar disembunyikan di semua container scroll (tab kategori, drawer, dropdown search/select, tabel admin, sku-picker) pakai 1 class `.no-scrollbar` reusable. Detail lengkap + commit range: memory `project_dannshop_repo.md`. Semua tsc/lint/test/build lolos, dikonfirmasi live via curl (HTTP status + isi HTML/CSS production, bukan cek visual — user yang pegang verifikasi visual mulai sesi ini).

**Perbaikan mobile layout (2026-08-05, sesi terpisah setelah A+B live):** user laporkan tombol hamburger/drawer & tombol chat CS "meluber" di mobile terutama saat zoom out, dan grid produk cuma 1 kolom di mobile (harusnya 3, referensi konteronline.id). Root cause overflow: search bar header adalah flex item tanpa `min-w-0`, `truncate` di dalamnya gagal menyusut, mendorong seluruh baris header 39px melebihi viewport. Fix: `min-w-0` di wrapper search bar + `overflow-x:hidden` di html/body sebagai jaring pengaman. Grid produk: `grid-cols-3` dijadikan default (sebelumnya cuma mulai `sm:` breakpoint ke atas = 1 kolom di mobile asli). Diverifikasi `document.scrollWidth` sebelum/sesudah (414px→375px) di dev DAN di production langsung. Commit `63e5833`.

**Insiden penting sesi ini, WAJIB dibaca sebelum sesi berikutnya nge-push apa pun ke `main`:** push Bagian B pertama (commit `9f34fc1`) sempat **gagal build di Vercel secara diam-diam** — production terus menyajikan build LAMA (Snap) tanpa ada tanda error yang terlihat user, karena `next build` mem-prerender halaman yang query tabel baru (`PaymentMethodConfig`, lalu `Banner`/`SiteSetting` dari Bagian A) yang **belum pernah di-`migrate deploy` ke database production** — proyek ini TIDAK auto-run migrasi di pipeline Vercel (`vercel.json` cuma set region, build command murni `next build`), migrasi production selalu manual dari lokal. Ketahuan karena `npm run build` lokal (yang otomatis load `.env.production` kalau ada) gagal persis dengan error sama. **Fix wajib tiap kali ada migration baru:** jalankan `prisma migrate deploy` dari lokal ke `DATABASE_URL` production SEBELUM atau SEGERA SETELAH push — kalau lupa, situs jalan diam-diam pakai kode basi tanpa error yang kelihatan (worst-case silent failure). Command `prisma migrate deploy` (DDL murni) lolos classifier keamanan auto-mode Claude Code; command custom (script Node ad-hoc, atau migration SQL yang isinya `INSERT` data) **diblokir classifier** dan harus dijalankan user sendiri via prefix `!` di chat — lihat memory `project_dannshop_repo.md` untuk detail command persis yang dipakai. Data seed (kategori 10 + 7 metode pembayaran) juga sempat ketinggalan di production karena sama-sama cuma ada di `prisma/seed.ts` yang dijalankan manual — sekarang sudah dipindah jadi migration SQL resmi (`20260805064622_seed_categories_and_payment_methods`, idempoten) supaya tidak terulang.

**Status Bagian B (2026-08-05) — 10/10 task selesai, 1 review independen (opus, bukan full SDD per-task karena deadline), 1 Critical + 1 Important ditemukan & di-fix:** migrasi checkout & deposit dari Midtrans Snap (popup) ke Core API inline (QRIS/VA 6 bank/Mandiri echannel dirender langsung di halaman invoice/status, tanpa popup). Fee + kode unik (Rp1-999) dihitung per metode, split `Deposit.amount` (dikreditkan wallet) vs `Deposit.totalPaid` (ditagih Midtrans) diverifikasi benar end-to-end lewat Midtrans sandbox asli (bukan mock) — wallet kekredit tepat `amount`, idempotent di-retry webhook. Fix wave (commit `4b2460a`): **Critical** — VA/echannel default expired ~24 jam di Midtrans vs job lokal expire di menit-15 (customer bisa bayar VA yang "sudah expired" di app tapi masih hidup di Midtrans, saldo tidak pernah kekredit otomatis) — fix `custom_expiry` disinkronkan ke semua charge, diverifikasi live expiry_time Midtrans persis sama dengan `expiredAt` lokal. **Important** — `Deposit.totalPaid` default 0 untuk deposit lama (dibuat sebelum kolom ini ada) bikin webhook nolak settlement asli — fix migrasi backfill, diverifikasi 0 row tersisa dengan totalPaid=0. Detail lengkap + hasil E2E per-step: `.superpowers/sdd/2026-08-02-pembayaran-inline-core-api/progress.md` (worktree sudah dihapus, tapi ledger ini tersimpan di `.superpowers/` root repo, bukan di worktree — cek dulu apakah masih ada sebelum baca).

**Status Bagian A (2026-08-05) — semua sub-bagian selesai, diverifikasi visual (Playwright screenshot) + fungsional end-to-end (upload banner, tandai produk trending) di dev lokal, lalu dikonfirmasi live di production:** model baru `Banner`+`SiteSetting`+`Product.isTrending`; kategori di-rename (Games→Top Up Game, Pulsa & Data→Pulsa, PLN→Token Listrik) + tambah 5 baru (Paket Internet, Telepon & SMS, Masa Aktif, Aktivasi Voucher, Tagihan) jadi 10 total, admin CRUD `/admin/categories` (hapus diblokir kalau kategori masih ada produk); banner carousel auto-slide 5 detik CSS scroll-snap murni + admin CRUD `/admin/banners`; section Trending manual/auto (order sukses 7 hari, fallback ke manual) diatur di `/admin/settings`; header logo dinamis (gambar/video dari `SiteSetting`, fallback teks) + search bar jadi trigger full-width di tengah header; marquee metode pembayaran aktif di atas footer (CSS keyframes, hormati `prefers-reduced-motion`); footer 4 kolom + 4 halaman statis baru (`/faq`, `/syarat-ketentuan`, `/kebijakan-privasi`, `/kontak`). Drive-by fix: `search-overlay.tsx` punya lint error pre-existing (`react-hooks/set-state-in-effect`) — diperbaiki karena file itu disentuh untuk redesign search bar, sekarang lint 0 error.

> **⚠️ SUPERSEDED 2026-08-06 — jangan diikuti lagi:** seluruh pendekatan Resend-via-env-var di bawah ini (Vercel Marketplace, atau `RESEND_API_KEY` manual di Vercel dashboard) sudah digantikan total oleh sistem konfigurasi email dari admin (Resend **atau** SMTP, tersimpan terenkripsi di database, diatur lewat Admin > Pengaturan Situs > Pengiriman Email). `RESEND_API_KEY`/`RESEND_FROM_EMAIL` sudah tidak dibaca kode manapun lagi. Bagian di bawah dibiarkan apa adanya sebagai riwayat, bukan instruksi yang masih berlaku — lihat entri "Terakhir update: 2026-08-06" di paling atas file ini.

**Status Bagian C (2026-08-05, terakhir diupdate akhir sesi) — semua kode selesai (email helper+template, wiring 3 pemicu, halaman /cek-transaksi, tombol share WA, buyerPhone field), tapi email BELUM AKTIF di production — sesi dijeda di sini atas permintaan user, HANDOFF POINT untuk sesi berikutnya:**
- **Percobaan 1 (Vercel Marketplace CLI) — DITINGGALKAN, jangan diulang.** `vercel integration add resend` sempat lolos 2 blocker CLI (terms acceptance di browser, lalu `-m domain=dsdigitalintegrasi.my.id -m region=us-east-1`), tapi provisioning akhirnya tetap butuh **verifikasi DNS domain lewat browser** yang gagal total di lingkungan CLI headless (`Additional setup required. Opening browser...` lalu exit 1, `vercel integration list` konfirmasi "No resources found"). User coba jalankan sendiri dari laptopnya juga tetap kena masalah (sepertinya soal plan/billing Vercel Marketplace, bukan browser) — **user memutuskan pindah jalur, jangan disarankan lagi coba Vercel Marketplace untuk Resend di sesi berikutnya.**
- **Percobaan 2 (langsung ke Resend.com, skip Vercel Marketplace) — jalur yang dipakai, SEDANG BERLANGSUNG saat sesi dijeda.** User daftar akun Resend langsung, dan **sudah mendapatkan 1 API key asli dari dashboard Resend** (di-paste user di chat sesi ini — TIDAK dicatat di sini/memory karena ini file yang di-commit ke git, jangan pernah tulis API key asli ke file manapun di repo). Assistant SENGAJA tidak menjalankan/menyentuh key itu lewat command apa pun (menghindari secret lewat command line, konsisten dengan pola classifier-block sebelumnya) — instruksi terakhir yang diberikan ke user: masukkan `RESEND_API_KEY` (value dari key yang sudah didapat) manual lewat dashboard Vercel (`vercel.com/dsdigital-s-projects/dannshop/settings/environment-variables`), centang scope Production+Preview+Development, lalu redeploy.
- **Belum dikonfirmasi selesai saat sesi ditutup** — sesi berikutnya WAJIB cek dulu: (1) apakah domain `dsdigitalintegrasi.my.id` sudah verified di dashboard Resend (SPF/DKIM), (2) apakah `RESEND_API_KEY` sudah benar-benar ada di Vercel env vars (`vercel env ls` — nama variabel doang, jangan pernah minta lihat isinya), (3) apakah sudah redeploy setelah env var ditambah. Kalau user bilang "udah kelar semua", tetap verifikasi lewat cara tidak langsung (misal trigger 1 checkout order beneran, cek apakah `Email: RESEND_API_KEY belum di-set` masih muncul di log Vercel Functions atau tidak) — jangan cuma percaya klaim, karena beberapa `!` command user sebelumnya di sesi ini sempat tidak menghasilkan perubahan nyata meski user mengira sudah jalan.
- **Reminder alamat pengirim:** `.env.example`/kode default `RESEND_FROM_EMAIL="DannShop <invoice@dsdigitalintegrasi.my.id>"` — pastikan address ini match domain yang benar-benar diverifikasi di Resend (kalau user pakai domain/subdomain lain, sesuaikan env var-nya, jangan biarkan mismatch bikin email ditolak Resend).
- Sampai semua ini selesai: checkout/fulfillment/dsb **tetap berfungsi normal 100%** (sudah diverifikasi E2E sebelumnya) — email cuma log `"Email: RESEND_API_KEY belum di-set, pengiriman dilewati"` dan dilewati, tidak mengganggu jalur uang sama sekali.
- Setelah `RESEND_API_KEY` aktif: **belum pernah dites kirim email sungguhan** (3 skenario: order dibuat, order sukses, order gagal/refund) — WAJIB diverifikasi manual begitu setup selesai, sebelum dianggap benar-benar tuntas.

**Belum dikerjakan / perlu tindak lanjut (non-blocking untuk demo):**
1. `NEXT_PUBLIC_MIDTRANS_CLIENT_KEY`/`NEXT_PUBLIC_MIDTRANS_IS_PRODUCTION` di Vercel Environment Variables (Production+Preview) sudah tidak dipakai lagi (Snap dihapus total) — aman dibiarkan (tidak bikin error), sebaiknya dihapus untuk kebersihan.
2. E2E full-payment-simulation Bagian B cuma dilakukan untuk QRIS dan VA BCA (representatif); BNI/BRI/CIMB/Permata/Mandiri cuma diverifikasi **charge berhasil dibuat**, bukan simulasi bayar penuh — risiko rendah, webhook sudah terbukti payment-method-agnostic.
3. Admin belum upload banner/logo asli di production (baru dites pakai file dummy di dev lokal) — sebelum demo, upload banner/logo beneran lewat `/admin/banners` dan `/admin/settings` kalau mau tampil optimal.
4. Resend domain verification (lihat "Status Bagian C" di atas) — satu-satunya blocker tersisa dari seluruh spec redesign storefront.

Sebelum itu, sesi 2026-08-02 ini (mode "eksekusi cepat", langsung ke `main`, sudah live di Production semua): navbar dipindah ke sidebar drawer, upload banner produk via Vercel Blob, bulk-import produk dari Digiflazz, fix latency checkout (Vercel function region `iad1`→`sin1` biar dekat TiDB Singapore), storefront full-width + kartu produk didesain ulang sesuai referensi.

## Status Migrasi Midtrans Snap (mulai 2026-08-01, selesai & merge 2026-08-02) — 6 TASK CODE + TASK 7 E2E + FINAL REVIEW SELESAI

Latar belakang: sebelumnya checkout & deposit cuma nawarin QRIS (Midtrans Core API `chargeQris`). Migrasi ke Snap (`createSnapTransaction`, `/snap/v1/transactions`) bikin popup Snap otomatis nawarin semua metode (QRIS, 10+ VA bank, kartu, ShopeePay) tanpa kerja tambahan per-metode. Full alur brainstorm→spec→plan→SDD dipakai (user eksplisit minta karena "nyentuh uang beneran"). Spec: `docs/superpowers/specs/2026-08-01-migrasi-midtrans-core-api-ke-snap-design.md`. Plan (7 task): `docs/superpowers/plans/2026-08-01-migrasi-midtrans-snap.md`. Dieksekusi via `superpowers:subagent-driven-development` di worktree `.claude/worktrees/migrasi-midtrans-snap-sdd`, branch `worktree-migrasi-midtrans-snap-sdd` (dari `main` sebelum plan-doc commit).

**Cakupan 6 task code:** Task 1 — fungsi client `createSnapTransaction` (`web/src/lib/midtrans/client.ts`), Zod-validated, reuse plumbing `chargeQris` yang sudah ada. Task 2 — load Snap.js via `<Script strategy="afterInteractive">` di `layout.tsx` + CSP di `next.config.ts` nambah host `app.sandbox.midtrans.com`/`app.midtrans.com` ke `script-src`/`connect-src`/`frame-src` (scoped, bukan wildcard). Task 3 — checkout wiring (`product-detail-client.tsx`, `actions/checkout.ts`). Task 4 — deposit wiring (`deposit-form.tsx`, `actions/deposit.ts`). Task 5 — tombol "Lanjutkan Pembayaran" di halaman invoice (`invoice-status.tsx`), reuse token Snap yang sudah tersimpan (tidak bikin transaksi baru). Task 6 — tombol sama di halaman status deposit (`deposit-status.tsx`), pola sama.

**Pola error-handling yang jadi standar di semua 4 titik pemicu popup Snap** (checkout, deposit-form, resume invoice, resume deposit-status): state `snapError` direset di awal handler/effect; pesan `"Gagal memuat metode pembayaran. Refresh halaman dan coba lagi."` kalau `!window.snap`; callback `onError` Snap set `"Pembayaran gagal diproses. Coba lagi atau pilih metode lain."`; pesan dirender dekat tombol. Brief asli task 3/5 tidak punya ini (celah plan-mandated), ketahuan di task review Task 3, langsung diterapkan proaktif di Task 4/6 begitu pola ketemu.

**Task 7 (E2E manual sandbox) — 5 langkah semua PASS, dikerjakan lintas 2 sesi:**
- Sesi 1: langkah 1 (dev server + checkout, popup render normal, 0 CSP violation baru) PASS. Langkah 2-5 BLOCKED — extension `claude-in-chrome` gak bisa klik ke dalam iframe cross-origin Snap (dicoba 8+ kali, semua elemen, 0 efek).
- Sesi 2: fix-nya pakai **Playwright asli** (`npm install playwright --no-save`, headed Chromium) — `page.mouse.click(x,y)` di level OS/compositor, bisa tembus iframe cross-origin, beda dari `computer` tool extension yang berbasis accessibility-tree. Pola yang sama dipakai Fase 4 Task 11 dulu — jadi default berikutnya kalau ada tugas butuh interaksi iframe cross-origin (Snap, OAuth popup, widget pembayaran lain), langsung ke Playwright, jangan coba extension dulu.
- QRIS disimulasikan via 2-step POST ke `simulator.sandbox.midtrans.com` (ambil `qrCodeUrl` dari `<img>` src asli popup → POST `/v2/qris/payment` → decode HTML-entity `referenceId`+`exploreData` → POST `/v2/qris/payment/gopay`). VA (dites BCA) beda pola: POST `va_number` ke `/bca/va/inquiry` → POST field tersembunyi ke `/bca/va/payment`. Webhook di-POST manual pakai `signature_key` ASLI dari `getTransactionStatus` (bukan bypass, exercise signature-verification code path betulan).
- Order: `PENDING_PAYMENT → PAID → PROCESSING → REFUND_PENDING` (fulfillment gagal karena kredensial Digiflazz dummy di dev DB, sesuai ekspektasi) — sama persis untuk QRIS maupun VA, konfirmasi `handleOrderWebhook` payment-method-agnostic. Deposit: wallet kekredit tepat 1x, 1 baris `WalletLedger` baru, tidak ada duplikasi.
- Resume-payment (invoice & deposit-status) diverifikasi reuse token Snap yang sama, tidak bikin transaksi Snap baru.

**Final whole-branch review (general-purpose subagent, range `b5349e6..01de312`): Ready to merge (0 Critical).** 1 Important — `NEXT_PUBLIC_MIDTRANS_CLIENT_KEY`/`NEXT_PUBLIC_MIDTRANS_IS_PRODUCTION` belum pernah ditambahin ke Vercel sama sekali (bukan cuma preview, production juga kosong) — kalau dibiarkan, popup Snap di app live bakal broken (`data-client-key` kosong). **Sudah di-fix SEBELUM review ini jalan** (lihat paragraf blocker Vercel di bawah), jadi status closed. 2 Minor diparkir (bukan gap): tombol resume-payment gak ada debounce klik ganda (UX minor, bukan risiko uang — token Snap yang dipakai sama aja); race kecil antara Snap.js load vs trigger cepat (sudah di-guard `!window.snap`, cuma false-negative jarang).

**3 blocker Vercel (dari sesi sebelumnya) — semua sudah di-fix di sesi ini, sebelum merge:**
1. Deployment Protection (SSO) di Preview — bukan di-fix (memang jangan dimatikan), cukup pakai `vercel curl` (auto-generate bypass token) buat verifikasi non-browser.
2. Preview environment kosong sama sekali dari semua env var → 14 variable ditambahin ke scope Preview (`DATABASE_URL`/`ADMIN_EMAIL`/`ADMIN_PASSWORD`/`MIDTRANS_SERVER_KEY` reuse nilai asli yang sama dengan Production karena belum ada DB staging terpisah; `AUTH_SECRET`/`CRON_SECRET`/`CREDENTIALS_ENCRYPTION_KEY` di-generate BARU khusus Preview, sengaja beda dari Production karena gak wajib sama — konsekuensinya kalau ada test di Preview yang perlu decrypt `ProviderConfig.credentials` yang udah ada di DB bakal gagal decrypt, itu diketahui & diterima). Setelah redeploy manual (`vercel redeploy ... --target preview`), homepage Preview konfirmasi **200 OK** (sebelumnya 500).
3. `NEXT_PUBLIC_MIDTRANS_CLIENT_KEY`/`NEXT_PUBLIC_MIDTRANS_IS_PRODUCTION`/`NEXT_PUBLIC_WHATSAPP_CS`/`NEXT_PUBLIC_TELEGRAM_CS` ditambahin ke scope Production juga (belum pernah ada sama sekali) — bakal aktif otomatis di deploy Production berikutnya (yaitu abis merge & push sesi ini).

**Gotcha proses yang ketemu sesi ini, layak diinget:** (a) Vercel CLI (58.4.4) sekarang sensor nilai env var jadi literal `[SENSITIVE]` kalau diakses lewat agent (`vercel env pull`/`ls`) — fitur keamanan baru khusus mendeteksi agent, bukan bug/setting akun. Efeknya: nilai secret yang cuma ada di dashboard Vercel (gak ada salinan lokal) SEKARANG GAK BISA dibaca ulang oleh Claude Code. (b) `vercel env add` dengan value asli (connection string DB, dst) sempat diblokir classifier auto-mode Claude Code sebagai kemiripan credential exfiltration — solusinya user jalanin sendiri command-nya via prefix `!` di chat. (c) Git Bash di Windows auto-convert argumen yang diawali `/` jadi path Windows (`/` → `C:/Program Files/Git/`) — bikin `vercel curl /` gagal aneh (`Malformed input to a URL function`); fix-nya prefix `MSYS_NO_PATHCONV=1`.

**Merge:** fast-forward `main` ← `worktree-migrasi-midtrans-snap-sdd` (commit `01de312`), 142/142 test hijau + `tsc --noEmit` bersih di checkout utama (tidak ada perubahan schema Prisma di migrasi ini, jadi tidak perlu `prisma generate` ulang).

## Status Fase 7d (2026-07-31) — 5 TASK + FINAL REVIEW (1 RONDE FIX) SELESAI, SUDAH MERGE

Audit sumber: `docs/superpowers/specs/2026-07-30-fase-7c-security-hardening-audit.md` (9 Medium + 6 Low, sisa dari audit yang sama dengan Fase 7c). Spec desain: `docs/superpowers/specs/2026-07-31-fase-7d-security-hardening-medium-low-design.md`. Plan (4 task implementasi + 1 verifikasi manual): `docs/superpowers/plans/2026-07-31-fase-7d-security-hardening-medium-low.md`. Dieksekusi via `superpowers:subagent-driven-development` di worktree `.claude/worktrees/fase-7d-security-hardening-medium-low-sdd`, branch `worktree-fase-7d-security-hardening-medium-low-sdd` (dari `main` commit `8fdc4a2`). Sesi eksekusi terpecah 2: Task 1 selesai duluan (istirahat user), Task 2-5 + final review + merge selesai sesi berikutnya (2026-07-31).

**Cakupan:** Task 1 — M-1 (CSP + security header lengkap via middleware) + M-2 (QR code self-generate server-side pakai `qrcode` npm package, ganti panggilan pihak ketiga `api.qrserver.com`). Task 2 — M-3 (nominal settlement webhook diverifikasi vs `order.total`/`deposit.amount` sebelum mark-paid/kredit — mismatch order eskalasi `NEEDS_REVIEW`, mismatch deposit blokir kredit) + L-2 (`safeCompare` timing-safe dipasang di verifikasi signature Midtrans) + L-4 (redaksi SN/voucher dari pesan error yang bisa nyasar ke log — sumber asli di `digiflazz.ts`, bukan `fulfillment.ts`/`runner.ts` seperti dugaan awal audit) + verifikasi ulang L-3 (header whitelist webhook, sudah tertutup sejak Fase 7c). Task 3 — M-5 (respons registrasi seragam isi DAN timing, cegah enumerasi email) + M-6 (seed tidak pernah promote user existing jadi admin) + M-7 (hapus `session!` assertion di 3 halaman `/account/*`) + L-1 (`CRON_SECRET` timing-safe, awalnya cuma di route handler — final review nemu proxy.ts juga perlu). Task 4 — M-4 (limit field `target` checkout: 255 char/10 field) + M-8 (pesan error admin generik, `console.error` server-side) + M-9 (`crypto.randomInt` ganti `Math.random()` di order/ref number) + L-5 (redaksi log Telegram) + L-6 (`Cache-Control: no-store` di 2 endpoint sisa). Task 5 — verifikasi manual E2E (controller sendiri, bukan subagent, lihat di bawah).

**2 keputusan didelegasikan user ke asisten (sebelum eksekusi mulai):** M-2 QR self-generate di server (bukan pindah ke pihak ketiga lain) — diambil; M-6 seed tidak pernah update role user existing — diambil.

**Task 3 fix round 1 (plan-mandated finding, user pilih via `AskUserQuestion`):** task reviewer nemu respons registrasi SUDAH seragam ISI (redirect sama persis di 3 cabang) tapi BEDA WAKTU proses (cabang admin-email/email-sudah-ada langsung redirect, cabang sukses baru bayar `bcrypt` cost-12 ~100-300ms) — bawaan brief/spec asli, bukan deviasi implementer. User pilih **fix sekarang**: cabang admin-email & email-sudah-ada ditambah `hashPassword` dummy supaya ketiga cabang bayar cost bcrypt yang sebanding.

**Insiden proses (2x, Task 4 & seterusnya) — implementer subagent salah nulis file ke checkout utama `D:\Coding VSC\DannShop-PPOB` (bukan ke worktree), meski instruksi eksplisit bilang cwd sudah benar:**
1. Task 4 percobaan pertama: implementer sempat 2x `git commit` ke `main` di checkout utama (bukan worktree) — main sempat punya 2 commit nyasar (`c6303a8`/`e8e1c68`) sebelum ketahuan. Fix: `main` di-reset `--hard` ke `8fdc4a2` (belum pernah di-push, aman), worktree dibersihkan (`git checkout HEAD -- .`), implementer di-redispatch dengan guard eksplisit (`git rev-parse --show-toplevel`+`git branch --show-current` WAJIB dicek sebelum commit) — percobaan kedua bersih.
2. Sebelum merge (Step "Detect Environment" di skill `finishing-a-development-branch`): ditemukan lagi 4 file (`crypto.ts`, `proxy.ts`, `cron/tick/route.ts`, `webhooks/midtrans/route.ts`) ter-edit (TAPI TIDAK ter-commit) di checkout utama — kontaminasi kumulatif dari agent Task 2/3/final-fix-wave yang isinya CAMPURAN beberapa versi draft (termasuk 1 varian `safeCompare` yang beda dari versi final ter-commit di worktree, kemungkinan sisa draft agent pertama Task 2 yang sempat di-kill). Karena `main` git log tetap bersih (`8fdc4a2`, tidak ada commit baru) dan isinya provably redundant/inferior vs versi ter-review di worktree, langsung di-`git checkout -- <files>` (discard, bukan commit apa pun yang hilang).

**Pelajaran untuk fase berikutnya:** subagent implementer yang dipatok ke worktree (baik lewat instruksi prompt maupun `isolation` param) TERBUKTI tidak selalu konsisten soal working directory untuk operasi file (Edit/Write tool) — commit git-nya sendiri sudah diverifikasi selalu benar (lolos guard `pwd`+`branch`), tapi *file writes* di sela-sela proses (sebelum commit final) bisa nyasar ke checkout utama. **WAJIB cek `git status --short` di checkout utama (`MAIN_ROOT`) tiap kali sebelum merge**, bukan cuma di worktree — jangan asumsikan checkout utama tetap bersih sepanjang sesi SDD berjalan.

**Task 5 (verifikasi manual E2E, dilakukan controller sendiri via curl + script tsx + Midtrans sandbox asli — bukan subagent, dev server `npm run dev` lokal):** Semua 4 langkah PASS. Header keamanan lengkap (`X-Frame-Options`, CSP, HSTS). QR invoice = `data:image/png;base64` murni, 0 request ke `qrserver`. `registerAction` (kode produksi asli, dipanggil langsung via tsx) diuji 4 skenario: email baru (buat user), email duplikat (redirect identik, no new user), `ADMIN_EMAIL` (redirect identik, no new user), validasi gagal (tetap `{error}`) — semua PASS. Seed dijalankan 2x, `updatedAt` admin **tidak berubah sama sekali**. `/account/*` tanpa sesi → 302, dengan sesi (login asli via NextAuth credentials flow) → 200 di ketiga halaman. **M-3 diverifikasi end-to-end via Midtrans sandbox ASLI**: order yang sudah pernah settle di-manipulasi `order.total`-nya di DB, webhook baru (signature valid, dihitung asli) di-POST manual — `getTransactionStatus` REAL ke Midtrans balikin nominal asli (22000), mismatch terdeteksi, `escalateOrder` dipanggil (no-op karena order test kebetulan `REFUND_PENDING`, status terlindungi `ORDER_STATUSES_NOT_OVERWRITABLE_BY_AUTOMATION` — perilaku benar, bukan bug), order **tidak pernah** jadi `PAID`.

**Final whole-branch review (opus, range `8fdc4a2..3ab072e`, 6 commit): Ready to merge: With fixes.** 0 Critical, 2 Important (keduanya cross-task gap yang cuma kelihatan di scope whole-branch, tidak kelihatan di review per-task manapun):
1. L-1 belum tertutup penuh — `web/src/proxy.ts` (middleware, jalan SEBELUM route handler) masih pakai `===` untuk `CRON_SECRET`, terpisah dari `route.ts` yang sudah `safeCompare` sejak Task 3.
2. Money-safety — mismatch nominal deposit cuma `console.error`, tidak ada alert Telegram (beda dari jalur order yang selalu alert via `escalateOrder`) — customer bisa bayar tanpa pernah dikreditkan DAN tanpa ada yang tahu kecuali baca log manual. Sejalan dengan preferensi standing user (money-safety = fix sekarang, tanpa kecuali) + rekomendasi reviewer sendiri, **langsung fix, tidak ditanya ulang ke user.**

9 Minor diparkir (tidak fix di fase ini, dicatat sebagai backlog): sisa `Math.random()` di 1 titik test-transaction admin (`providers.ts:155`, M-9 residual); 2 raw `e.message` masih ke admin UI (`fulfillment.ts:353,438`, M-8 residual); sub-milidetik residual timing di cabang admin-email `registerAction` (di bawah noise jaringan); `eventKey` per `transaction_status` bisa double-escalate+double-alert (operator noise); `escalateOrder` M-3 tanpa `fromStatus` (history kehilangan konteks); `QRCode.toDataURL` unguarded di 2 halaman (throw=500, QRIS payload jauh di bawah kapasitas QR); CSP belum ada `base-uri`/`form-action`; `/account/deposit` pakai `notFound()` sementara 3 halaman M-7 pakai `redirect` (UX beda, sama-sama aman); `qrcode@1.5.4` bawa `yargs@15` dkk ke production deps (dead weight, tidak ada CVE aktif). Rekomendasi reviewer: kalau audit doc di-update lagi nanti, catat M-8/M-9 sebagai "partial closed" (tidak dikerjakan sesi ini).

Fix wave (commit `695f5b0`, base `3ab072e`): kedua Important diperbaiki 1 commit (`proxy.ts` pakai `safeCompare` + `handleDepositWebhook` kirim `sendTelegramAlert` sebelum `return "amount_mismatch"`). Scoped re-review (sonnet): **semua ADDRESSED, tidak ada breakage baru.**

**Merge:** fast-forward `main` ← `worktree-fase-7d-security-hardening-medium-low-sdd` (commit `695f5b0`), 139/139 test hijau + `tsc --noEmit` bersih di checkout utama setelah `npm install` + `prisma generate` ulang. Worktree gagal dihapus otomatis (`Device or resource busy`, Windows, sesi masih pin ke direktori itu) — fix: `ExitWorktree action:"keep"` dulu buat lepas pin, baru `rm -rf` manual + `git worktree prune`. Branch feature dihapus.

**Semua 9 Medium + 6 Low dari audit `docs/superpowers/specs/2026-07-30-fase-7c-security-hardening-audit.md` sekarang TERTUTUP** (gabungan Fase 7c yang sudah tutup 2 Critical + 4 High, dan Fase 7d yang tutup sisanya) — kecuali 2 residual minor (M-8/M-9 partial, lihat parkir di atas) yang tersisa sebagai backlog kecil, bukan gap keamanan aktif.

## Status Fase 7c (2026-07-30 s.d. 2026-07-31) — 8 TASK + FINAL REVIEW (1 RONDE FIX) SELESAI, SUDAH MERGE

Audit: `docs/superpowers/specs/2026-07-30-fase-7c-security-hardening-audit.md`. Spec desain final: `docs/superpowers/specs/2026-07-30-fase-7c-security-hardening-design.md`. Plan (8 task): `docs/superpowers/plans/2026-07-30-fase-7c-security-hardening.md`. Dieksekusi via `superpowers:subagent-driven-development` di worktree `.claude/worktrees/fase-7c-security-hardening-sdd`, branch `worktree-fase-7c-security-hardening-sdd` (dari `main` commit `717a90b`). Sesi eksekusi terpecah 2: Task 1 selesai duluan (istirahat user), Task 2-8 + final review + merge selesai sesi berikutnya (2026-07-31).

**Scope: 2 Critical + 4 High dari audit.** C-1 (poisoning idempotency webhook Midtrans) + H-2 (webhook storage exhaustion, fix sama): signature diverifikasi PALING AWAL sebelum `WebhookEvent` disentuh sama sekali, plus body-size cap & header whitelist. H-1: rate limiter `checkRateLimit`/`extractIp` berbasis tabel `RateLimit` (fail-open penuh di semua jalur DB-error, termasuk race `updateMany`), dipasang di proxy (login/register/webhook/cron-tick/order-status) + checkout guest + per-email login. **C-2 (backlog lama sejak Fase 4, akhirnya ditutup):** invoice + status API pindah dari kunci `orderNumber` (4 digit/hari, enumerable) ke `Order.publicToken` (cuid unik, unguessable) — route lama `[orderNumber]` dihapus total, tanpa fallback. H-3: kill-switch `ProviderConfig.isActive` sekarang benar-benar dihormati di `getAdapter`/`selectFulfillmentSku`/`isItemPurchasable`/checkout/fulfillment. H-4: sesi admin `maxAge` 8 jam + revalidasi wajib via snapshot `User.updatedAt` di 3 salinan `requireAdmin()` + gate `/admin` di proxy (task review menemukan 1 titik lagi yang kelewat brief: `GET /api/admin/provider-price-list`, langsung diperbaiki).

**Task 8 (verifikasi E2E manual) — dijalankan TANPA browser sama sekali.** User remote (`/rc`), tidak bisa akses browser, dan Chrome extension gagal ter-install/connect sesi ini (jangan disarankan lagi sesi ini). Controller yang menjalankan seluruh Task 8 sendiri: curl untuk webhook/rate-limit/route access, script Node/tsx langsung ke dev DB untuk seed data test + toggle provider + demote admin, dan panggilan asli ke Midtrans sandbox API (termasuk QRIS payment simulator `https://simulator.sandbox.midtrans.com/v2/qris/index` — 2 langkah: POST `qrCodeUrl` ke `.../payment` dapat form konfirmasi kedua berisi `referenceId`+`exploreData`, POST lagi ke `.../payment/gopay` buat settle beneran). Semua 5 langkah PASS, termasuk order asli lunas via Midtrans sandbox (kredensial: Merchant ID `M529124102`, ada di `.env` — lihat memory `project_dannshop_repo` untuk detail teknik simulator kalau perlu diulang). DB dev tidak ada produk purchasable sama sekali (seed sengaja `isActive=false` semua) — controller seed 1 produk dummy (Mobile Legends + kredensial DIGIFLAZZ palsu) buat keperluan test, state lokal saja, tidak dikomit.

**Final whole-branch review (opus): Ready to merge: With fixes.** 1 Critical (money-safety) + 3 Important, SEMUA interaksi lintas-task yang tidak kelihatan dari review per-task:
1. **Critical:** kill-switch bikin job `recheck-fulfillment` (cek status order yang SUDAH dibayar & terkirim ke provider) ikut throw kalau provider dinonaktifkan — order macet `PROCESSING` selamanya, tanpa alert, tanpa jalur admin. Fix: `getAdapter` dapat opsi `allowInactive` khusus jalur cek-status (bukan dispatch baru), job handler dibungkus try/catch yang eskalasi ke `NEEDS_REVIEW` alih-alih throw ke mesin retry job yang buta.
2. Rate limit `/login`+`/register` ke-trigger prefetch `<Link>` production (GET), bukan cuma submit asli — fix: gate 2 rule itu ke method POST saja.
3. Limit `order-status` (30/menit) terlalu ketat vs polling invoice tiap 3 detik — dinaikkan ke 120/menit.
4. `extractIp` percaya entry PERTAMA `X-Forwarded-For` (bisa dipalsukan penyerang di belakang reverse proxy) — harusnya entry TERAKHIR (hop tepercaya dari proxy Hostinger). Fix + tambahan: `cron-tick` di-exempt dari rate-limit IP kalau `x-cron-secret` sudah valid (endpoint itu sudah terautentikasi via secret, tidak butuh lapisan IP yang malah jadi celah DoS-spoofing).

Fix wave 1 commit (`a38e609`), scoped re-review (sonnet): semua ADDRESSED, tidak ada breakage baru. 1 Minor out-of-scope dicatat (perbandingan `CRON_SECRET` pakai `===` biasa bukan timing-safe — pola lama, bukan regresi fix wave ini).

**Merge:** fast-forward `main` ← `worktree-fase-7c-security-hardening-sdd` (commit `a38e609`), 130/130 test hijau + `tsc --noEmit` bersih di checkout utama **setelah `prisma generate` ulang** (Prisma Client checkout utama basi krn schema berubah — regenerate WAJIB tiap merge yang bawa perubahan schema, jangan asumsikan otomatis). Worktree sempat gagal dihapus (`Invalid argument`, sisa proses `node.exe` dev-server lama masih pegang file handle) — fix: kill proses node yang nyangkut dulu, baru `rm -rf` manual + `git worktree prune`. Branch feature dihapus.

**Backlog IDOR lama (sejak Fase 4) resmi TERTUTUP di fase ini** — jangan catat lagi sebagai open item di fase berikutnya.

## Status Fase 7b (2026-07-30) — 7 TASK + FINAL REVIEW (1 RONDE FIX) SELESAI, SUDAH MERGE

Plan: `docs/superpowers/plans/2026-07-30-fase-7b-alert-saldo-provider.md` (7 task). Spec: `docs/superpowers/specs/2026-07-30-fase-7b-alert-saldo-provider-design.md`. Dieksekusi via `superpowers:subagent-driven-development` di worktree `.claude/worktrees/fase-7b-alert-saldo-provider-sdd`, branch `worktree-fase-7b-alert-saldo-provider-sdd` (dari `main` commit `b4b04f6`). Sesi eksekusi terpecah 2: Task 1-5 selesai, dijeda mid-Task 6 (istirahat user); Task 6-7 + final review + fix wave selesai sesi berikutnya.

**Fitur:** job `check-provider-balance` (self-reschedule 1 jam) memantau saldo tiap provider topup, mengirim alert Telegram edge-triggered (⚠️ menipis / ✅ pulih) berdasarkan ambang batas per-provider yang bisa dikonfigurasi admin di `/admin/providers` (`minBalanceAlert`, nullable = alert nonaktif).

**Task 1-6:** semua clean/fix-round beres. Insiden proses Task 1 (implementer sempat commit ke `main` checkout utama karena worktree belum `npm install`) — diperbaiki via reconciliation git manual, tidak terulang. Task 4 implementer sengaja pakai `superRefine` bukan `z.union`-in-pipe literal brief — reviewer verifikasi independen brief literal justru melanggar spec-nya sendiri di Zod v4, disetujui. Task 5 implementer sengaja tidak ikuti asumsi import brief karena `runner.ts` sudah direfactor Fase 7a — brief ditulis dari baca basi, deviasi benar. Task 6 (form ambang alert di `provider-card.tsx`) 1 Minor deferred (`bg-muted/30`, opacity fraction baru).

**Task 7 (verifikasi E2E manual, laporan `.superpowers/sdd/2026-07-30-fase-7b-alert-saldo-provider/task-7-report.md`, satu-satunya file arsip yang sengaja di-force-add sebelum workspace dihapus):** build + 117/117 test + tsc + lint semua bersih. Jalur menipis→no-repeat→pulih diverifikasi **PENUH via kredensial Digiflazz asli + bot Telegram asli** (`t.me/dannshop_bot` — bot lama dari Fase 7a tidak lagi punya kredensial tersimpan di `.env` manapun, jadi **bot baru dibuat user sesi ini**, token+chat ID ditambahkan ke `.env` worktree; kredensial ini belum disinkronkan ke `.env` checkout utama — lihat catatan "tindak lanjut" di bawah). Ditemukan proses: job self-reschedule 1 jam berarti tick berikutnya tidak otomatis re-run job yang sama — untuk uji ulang harus paksa `Job.runAt` jadi lampau via query langsung (bukan bug, cuma kebutuhan testing). Step 6 (jalur gagal-cek/`healthStatus: DOWN`) **di-skip** atas keputusan user — mengubah kredensial Digiflazz asli untuk simulasi dinilai terlalu berisiko (percobaan backup blob terenkripsi diblokir classifier keamanan sistem), logic-nya sendiri sudah diverifikasi benar dari kode (bukan live test) saat final review.

**Final whole-branch review (opus, range `b4b04f6..a8ed474`, 8 commit): Ready to merge: With fixes.** Tidak ada Critical. 2 Important, user diminta pilih via `AskUserQuestion`:
1. **I-1**: `saveBalanceThreshold` tidak reset `balanceAlertStatus` saat ambang diubah — status `LOW` lama bikin alert "menipis" berikutnya senyap total, atau menurunkan ambang saat `LOW` memicu alert "pulih" palsu (terbukti nyata dipakai Task 7 Step 5 sendiri untuk simulasi pulih). User pilih fix: reset ke `"OK"` di update yang sama.
2. **I-2**: status di-persist ke DB SEBELUM konfirmasi Telegram terkirim (`sendTelegramAlert` didesain tidak pernah throw) — kegagalan kirim bikin transisi status hilang tanpa retry. Pola ini sama persis `escalateOrder` Fase 7a, tapi di sini pesan Telegram adalah deliverable utama (beda dengan order yang punya `/admin/orders` sebagai jaring kedua). User pilih **fix** (bukan terima pola Fase 7a apa adanya): `sendTelegramAlert` jadi `Promise<boolean>`, runner cuma persist status kalau kirim sukses.

2 Minor dibundel ke fix wave yang sama (user pilih bundling karena file sama sudah dibuka): **M-1** update `balanceAlertStatus` tidak atomik (fix: CAS via `updateMany`, dieksekusi setelah kirim sukses bukan sebelum, supaya konsisten I-2) dan **M-4** badge "Sehat" salah tampil di provider Nonaktif yang sebenarnya tidak pernah dipantau job (fix: gate badge dengan `isActive`, tampilkan "Tidak dipantau" kalau nonaktif).

Fix wave (commit `90ed0b1`, base `a8ed474`): keempat temuan diperbaiki 1 commit, 117/117 test tetap PASS, tsc bersih, lint 0 error. Re-review scoped (sonnet): **semua 4 ADDRESSED, tidak ada breakage baru, tidak ada out-of-scope observation.**

**7 Minor lain di-defer non-blocking** (dicatat ledger, sudah dihapus bareng workspace — ringkasan): duplikasi row job kalau `Job.update(DONE)` gagal setelah row jadwal berikutnya dibuat (pre-existing pola `sync-prices`, bukan regresi Fase 7b); `ProviderBalanceLog` write-only tanpa retensi (tidak pernah dibaca di manapun, kandidat backlog grafik tren saldo); form ambang tidak ikut `actionsDisabled` (aman fungsional, cuma sedikit tidak konsisten); hardening input `saveBalanceThreshold` (key invalid/nilai BigInt raksasa/hex literal — admin-only, dampak nihil); `providers.ts:50` cast `as string` redundan; warning dev-only Base UI uncontrolled→controlled field (non-fungsional, bukan unik Task 6).

**Merge:** fast-forward `main` ← `worktree-fase-7b-alert-saldo-provider-sdd` (commit `65090a3`), 117/117 test tetap hijau post-merge, branch feature dihapus, worktree dihapus bersih (tidak ada sisa lock, beda dari Fase 7a).

**Tindak lanjut non-blocking setelah merge:** sinkronkan `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID`/`NEXT_PUBLIC_APP_URL` (bot baru sesi ini) ke `.env` checkout utama (`D:/Coding VSC/DannShop-PPOB/web/.env`) — saat ini hanya ada di `.env` worktree, dan tanpa `NEXT_PUBLIC_APP_URL` link di pesan alert Telegram jadi path relatif yang tidak bisa diklik.

## Status Fase 7a (mulai 2026-07-29, selesai & merge 2026-07-30) — 8 TASK + FINAL REVIEW (2 RONDE) SELESAI

Plan: `docs/superpowers/plans/2026-07-29-fase-7a-admin-orders-refund.md` (8 task). Spec: `docs/superpowers/specs/2026-07-29-fase-7a-admin-orders-refund-design.md`. Dieksekusi via `superpowers:subagent-driven-development` di worktree `.claude/worktrees/fase-7a-admin-orders-refund-sdd`, branch `worktree-fase-7a-admin-orders-refund-sdd`. Sesi eksekusi terpecah 2: Task 1-8 selesai 2026-07-29 (dijeda sebelum re-review terakhir Task 8), final review + merge selesai 2026-07-30.

**Fitur:** halaman admin `/admin/orders` (daftar + filter tab + pencarian) dan `/admin/orders/[orderNumber]` (detail + 4 aksi: retry fulfillment, retry refund ke saldo, tandai selesai manual, tandai sudah direfund manual), notifikasi Telegram otomatis ke admin tiap kali order jatuh ke `NEEDS_REVIEW`/`REFUND_PENDING` (bot asli `t.me/dannshop_bot`), retry logic (`decideFulfillmentRetry`) untuk fulfillment gagal.

**Task 1-8:** semua clean/fix-round beres (lihat isi lama dokumen ini di git history untuk detail tiap task kalau perlu). 2 bug produksi nyata ditemukan saat verifikasi Task 8: RSC boundary break (`order-actions.tsx` Client Component import server action inline, dilarang Next.js 16 — fix pola actions-as-props) dan `OrderStatusHistory.note` VARCHAR(191) overflow yang bikin `sendTelegramAlert` diam-diam skip (fix: helper `truncateNote()`).

**Final whole-branch review (opus, range `3626d00..f0f9433`, 17 commit): Ready to merge: With fixes.** 1 Critical + 4 Important ditemukan, semua diputuskan fix-sekarang oleh user (2 di antaranya butuh keputusan eksplisit lewat `AskUserQuestion` karena trade-off produk/scope):
1. **Critical — double payout**: `markCompletedManualAction` bisa dipanggil saat order masih `PROCESSING` (fulfillment attempt masih live + job recheck terjadwal); kalau job itu resolve belakangan, semua order-status write di `applyFulfillmentResult`/`runner.ts` yang TIDAK TERJAGA bisa menimpa `COMPLETED` jadi `REFUNDED`/`REFUND_PENDING` (member: saldo ke-kredit dobel). Fix: constant `ORDER_STATUSES_NOT_OVERWRITABLE_BY_AUTOMATION`, semua write jadi guarded `updateMany`, transaksi refund-saldo abort dgn sentinel `ORDER_ALREADY_TERMINAL` kalau kalah race, + eligibility gate baru di `markCompletedManualAction` (tolak kalau attempt terakhir masih `SENT`/`PROCESSING`).
2. **Important — note overflow lagi**: 2 lokasi baru di `orders.ts` (SN manual, catatan refund manual) belum pakai `truncateNote()`. Fix: extract ke `web/src/lib/order/status-note.ts` (surrogate-pair-safe), pasang di 3 lokasi yang kelewat + `maxLength` hint di textarea admin.
3. **Important — re-alert redundan**: retry manual admin yang gagal lagi tetap kirim alert Telegram, padahal Global Constraint plan bilang cuma transisi otomatis yang boleh alert. User pilih fix: parameter `alertOnFailure` di `selectAndSend` (default true, admin retry pass false).
4. **Important — jaminan notifikasi rapuh secara struktural**: urutan `db.order.update` → `orderStatusHistory.create` → `sendTelegramAlert` bikin gagal DB write manapun diam-diam skip alert. Fix: helper `escalateOrder()` yang jamin alert tetap terkirim kalau status-claim berhasil, walau history write throw.
5. **Important — SN manual tidak sampai invoice pembeli**: user pilih approve migrasi Prisma kecil (`Order.manualSn String? @db.Text`, sengaja `@db.Text` bukan default VARCHAR(191)) setelah opsi migration-free (reuse `OrderFulfillment.provider`/`Order.target`) dinilai secara semantik salah.

Re-review ronde 1 (opus) atas fix wave: findings 1-4 ADDRESSED bersih, TAPI nemu 2 bug baru dari fix itu sendiri — Critical (migrasi `manualSn` salah nama tabel, `order` bukan `Order`, bakal gagal `prisma migrate deploy` di MySQL Linux case-sensitive kayak target Hostinger) + Important (endpoint polling status `/api/orders/[orderNumber]/status` belum ikut di-update, SN masih `null` buat pembeli yang invoice-nya live-polling). Kedua fix mekanis 1-baris ini di-dispatch terpisah (commit `ff82a7f`), re-review ronde 2 (sonnet) atas fix ini: ADDRESSED bersih, tidak ada breakage baru — verifikasi casing table-name terhadap SEMUA referensi tabel di migrasi init, verifikasi field `manualSn` benar-benar ke-select di runtime bukan cuma cocok tipe.

**Minor findings dari kedua ronde final review, semua diparkir (non-blocking, rekomendasi reviewer sendiri):** silent guard-loss pada double-delivery cuma `console.error` tanpa channel notify (bukan regresi, perilaku sama dengan sebelum fix); `truncateNote` (di `status-note.ts`) belum ada unit test otomatis walau kini pure function ter-ekspor (gap konvensi repo, risiko rendah, sudah diverifikasi benar via eksekusi langsung saat re-review); eligibility gate Fix 1b mempersempit opsi admin di kasus order stuck 30x-recheck (bukan deadlock, cuma sudut operasional baru); `retryOrderFulfillment`'s `not_eligible` branch (`fulfillment.ts:299`) masih 1 unguarded write, butuh 2 aksi admin konkuren buat kejadian, fix 1-baris kalau mau dikerjakan nanti.

**Merge:** fast-forward `main` ← `worktree-fase-7a-admin-orders-refund-sdd` (commit `fd83345`), 104/104 test tetap hijau post-merge, branch feature dihapus. Worktree-nya sendiri ketinggalan sedikit sisa file yang gagal dihapus (`Device or resource busy` di Windows, bukan proses node/vitest — sumbernya belum teridentifikasi, folder `.claude/worktrees/fase-7a-admin-orders-refund-sdd` masih ada di disk tapi TIDAK lagi terdaftar di `git worktree list`, jadi aman diabaikan/dihapus manual kapan saja setelah proses yang mengunci filenya berhenti).

Backlog belum dikerjakan (dicatat, bukan bagian scope Fase 7a): IDOR class di `/invoice/[orderNumber]` + endpoint status-nya (sudah ada sejak Fase 3, `orderNumber` cuma 4 digit/hari, enumerable) — sudah diflag sejak review final Fase 4, masih belum digarap, kandidat kuat utk salah satu sub-fase 7b/7c/7d berikutnya kalau ingin ditutup bareng pekerjaan admin/refund lain.

Setelah ini: lanjut Fase 7b (job alert saldo + notifikasi email) atau sub-fase 7c/7d lain — belum ada spec untuk itu.

## Dokumen kunci

- Design doc: `docs/superpowers/specs/2026-07-19-dannshop-topup-platform-design.md` (§12 = tabel roadmap Fase 1-7, §15 = addendum keputusan review 2026-07-24)
- Rencana Fase 1: `docs/superpowers/plans/2026-07-19-fase-1-fondasi.md`
- Rencana Fase 2: `docs/superpowers/plans/2026-07-24-fase-2-katalog-digiflazz.md` (14 task, katalog + integrasi Digiflazz)
- Rencana Fase 3: `docs/superpowers/plans/2026-07-25-fase-3-order-midtrans.md` (13 task: token desain Arah A, dark/light toggle, katalog publik, checkout, Midtrans, webhook, fulfillment, invoice+polling, verifikasi E2E) — **SEMUA 13 TASK SELESAI DIEKSEKUSI** (lihat "Status Fase 3" di bawah)
- Ledger eksekusi: `.superpowers/sdd/2026-07-25-fase-3-order-midtrans/progress.md` (jangan dihapus)
- **Branch Fase 1 & 2 SUDAH DIHAPUS** (lokal & remote, sudah di-merge ke `main` secara lokal — bukan lewat PR, karena ternyata tidak ada PR yang pernah benar-benar dibuat/di-merge di GitHub meski catatan sesi lalu mengira begitu). Branch kerja saat ini: **`fase-3-order-midtrans`** (dibuat dari `main` yang sudah update).

## Status Fase 4 (2026-07-25 mulai, 2026-07-29 selesai total) — 11 TASK + TASK 4b + FINAL REVIEW SELESAI

Rencana: `docs/superpowers/plans/2026-07-25-fase-4-member-deposit.md` **versi worktree** (direvisi 2x selama eksekusi — lihat poin di bawah, plan versi `main` sudah usang). Spec: `docs/superpowers/specs/2026-07-25-fase-4-member-deposit-design.md`. Dieksekusi via `superpowers:subagent-driven-development` di worktree `.claude/worktrees/fase-4-member-deposit-sdd`, branch `worktree-fase-4-member-deposit-sdd`. Sesi eksekusi terpecah 2: Task 1-6+4b selesai 2026-07-25, dijeda istirahat; Task 7-11 + final review selesai 2026-07-29.

**Fitur:** deposit saldo via QRIS Midtrans (`/account/deposit`), bayar checkout pakai saldo (alternatif QRIS), auto-refund ke saldo untuk member kalau fulfillment gagal (guest tetap `REFUND_PENDING`, tidak berubah), dashboard member (`/account`, `/account/orders`, `/account/deposits`).

**2 revisi plan selama eksekusi (2026-07-25):**
1. Commit `befc82d` menyisipkan **Task 4b** (job `reconcile-paid-orders`) setelah review Task 4 menemukan gap uang nyata: order bayar-saldo bisa macet `PAID` selamanya kalau proses crash tepat antara commit transaksi debit dan panggilan `dispatchFulfillment`. User pilih fix sekarang.
2. Commit `d17a175` merombak **Task 5** pakai skill `ui-ux-pro-max` + `frontend-design` (komponen shared baru `radio-group.tsx` base-ui, bukan radio HTML mentah) setelah user menegur dispatch pertama yang masih pakai markup polos; Task 9 juga dipoles standar aksesibilitas yang sama (touch target ≥44px, `focus-visible:ring`, `aria-pressed`).

**Task 7-11 (sesi 2026-07-29) — 2 gap uang lagi ditemukan & diperbaiki lewat task review, keduanya plan-mandated (kode dari brief), user diminta pilih fix-sekarang vs terima-gap, keduanya pilih fix sekarang:**
- Task 8 (webhook deposit): klaim status `PENDING→PAID` awalnya di luar `db.$transaction` yang mengkredit saldo — kalau crash di antara keduanya, deposit macet `PAID` tanpa saldo masuk & retry webhook silent-skip. Fix: pindah klaim ke dalam transaksi yang sama (commit `844d183`).
- Task 9 (halaman + endpoint status deposit): tidak ada cek kepemilikan (`deposit.userId` vs sesi) — IDOR-shaped, siapa saja yang tahu `depositId` bisa lihat nominal/status deposit member lain. Fix: tambah `auth()` + cek `deposit.userId`, 404 tidak membedakan not-found vs not-yours (commit `3da11c7`).

**Task 11 (verifikasi akhir E2E):** semua 8 sub-langkah **PASS**, termasuk 2 invariant paling kritis dengan kode produksi asli 100% (hanya panggilan jaringan Midtrans yang disintesis, source code tidak diubah): ledger refund order-saldo persis 2 baris tanpa duplikasi (`ORDER_PAYMENT` -X lalu `REFUND` +X, `balanceAfter` cocok saldo aktual), dan regresi guest tetap `REFUND_PENDING` (bukan `REFUNDED`) — Task 6 tidak mengubah perilaku guest checkout Fase 3. Playwright MCP tidak tersedia di sesi ini, diganti library `playwright` npm langsung (browser Chromium asli, hasil setara). Kredensial Midtrans sandbox ASLI masih belum ada (gap pre-go-live sama seperti Fase 3, non-blocking). Laporan lengkap (dikomit): `.superpowers/sdd/2026-07-25-fase-4-member-deposit/task-11-report.md` — **catatan: seluruh direktori `.superpowers/sdd/*` di-gitignore kecuali file yang eksplisit di-`git add -f`, jadi ledger & brief task akan hilang begitu worktree dibersihkan; hanya laporan yang sengaja di-force-add yang bertahan.**

**Final whole-branch review (opus, range `d276f7a..e646a0d`, 19 commit):** Ready to merge: With fixes. **Invariant ledger double-entry LULUS 100%** — audit menyeluruh menemukan tepat 3 situs mutasi `Wallet.balance` (`checkout.ts` debit, `fulfillment.ts` refund, webhook deposit kredit), semuanya atomik dalam `$transaction` bareng `WalletLedger` ber-`idempotencyKey` unik. 2 Important baru ditemukan (interaksi lintas-task yang tidak terlihat dari scope task manapun sendirian, bukan plan-mandated) & langsung diperbaiki (commit `87bd581`):
1. Job `reconcile-paid-orders` (satu-satunya jaring pengaman order saldo macet `PAID`) bisa macet `RUNNING` selamanya kalau proses mati di tengah 20 panggilan fulfillment berurutan (~300 detik worst-case), dan `ensureRecurringJobs` menganggap job `RUNNING` masih hidup selamanya — jaring pengaman mati diam-diam tanpa alert. Fix: `take` 20→5 (muat dalam budget waktu wajar), + `ensureRecurringJobs` anggap `RUNNING` basi kalau `updatedAt` >10 menit (pakai field `@updatedAt` yang sudah ada, tanpa migrasi).
2. Webhook deposit menelan diam-diam settlement "paid" yang datang setelah deposit lokal sudah `EXPIRED`/`FAILED` (race jam kadaluarsa lokal vs Midtrans, karena `chargeQris` tidak kirim `custom_expiry`) — uang masuk Midtrans, saldo tidak pernah bertambah, nol jejak audit. Fix: baca ulang status kalau klaim gagal, log error + `WebhookEvent.processResult="paid_but_not_pending"` (bukan diam-diam `"paid"`) kalau bukan duplikat sah dari deposit yang sudah `PAID`.

Re-review scoped (sonnet) atas fix wave: **semua ADDRESSED, tidak ada breakage baru.** 1 minor residual (belum ada test khusus untuk 2 branch baru) diparkir non-blocking.

**2 temuan Important lain dari final review SENGAJA DIPARKIR** (reviewer sendiri merekomendasikan tidak diperbaiki di branch ini):
- Order bayar-saldo bisa yatim permanen di `PENDING_PAYMENT` (tanpa `expiredAt`/job `expire-order`) kalau `db.$transaction` di `checkout.ts` throw error non-`InsufficientBalance` (infra/koneksi DB) — frekuensi rendah, perbaikan butuh sedikit desain (perluas `reconcile-paid-orders` pakai `WalletLedger` sebagai source of truth). Boleh dikerjakan setelah merge.
- Kelas kerentanan IDOR yang sama seperti Task 9 ternyata SUDAH ADA sejak Fase 3 di `/invoice/[orderNumber]` + `/api/orders/[orderNumber]/status` (orderNumber cuma 4 digit/hari, bisa dienumerasi, expose `sn`/total tanpa auth) — bukan regresi Fase 4, tapi jadi lebih relevan sekarang karena Fase 4 attach `userId` ke SEMUA order termasuk jalur QRIS. Reviewer eksplisit: JANGAN diperbaiki di branch ini (di luar scope, berisiko menyentuh jalur guest Fase 3 menjelang merge) — **catat ke backlog Fase 7** (bareng admin/refund queue).

4 Minor baru dicatat non-blocking (utang teknis kapan saja): `deposit-status.tsx` duplikat `STATUS_LABEL` vs `status-labels.ts`; `formatRupiah` ternyata terduplikasi 5x (bukan 3x seperti perkiraan awal); hasil `reconciled=N` dari `reconcile-paid-orders` dibuang tanpa disimpan (nol observability); kemungkinan starvation ringan reconcile vs job lain di antrean `runDueJobs`.

**Langkah berikutnya:** `superpowers:finishing-a-development-branch` untuk integrasi branch (`worktree-fase-4-member-deposit-sdd` → `main`) — BELUM dijalankan.

## Status Fase 3 (mulai 2026-07-25) — 13 TASK IMPLEMENTASI + TASK 13 VERIFIKASI AKHIR SELESAI

### Task 13 — Verifikasi akhir end-to-end (selesai, sesi 2026-07-25)

Laporan lengkap: `.superpowers/sdd/2026-07-25-fase-3-order-midtrans/task-13-report.md` (WAJIB dibaca sebelum final whole-branch review — berisi rincian tiap langkah + mana yang tervalidasi penuh vs parsial).

**Kredensial Midtrans sandbox ASLI tidak tersedia** di sesi ini (butuh signup manual pemilik produk) — dipakai strategi verifikasi pengganti (server key palsu konsisten + shim eksternal sementara untuk konfirmasi status Midtrans, TIDAK mengubah source code apa pun) supaya logic tetap tervalidasi. Ringkasan:

- **Automated**: `npx vitest run` 80/80 PASS, `npx tsc --noEmit` bersih, `npm run build` sukses (semua route baru ter-generate). **`npm run lint` GAGAL** — 1 error baru ditemukan (lihat Temuan #1 di bawah).
- **Manual FULL** (kode produksi asli, tanpa sintesis): checkout gagal-charge → order `FAILED` bukan macet `PENDING_PAYMENT` (fix Task 9 terbukti benar); webhook dedup (kirim 2x payload sama → `{deduped:true}`, tidak dobel dispatch); job `recheck-fulfillment` via `POST /api/cron/tick`; job `expire-order` via cron tick; signature invalid → 403; cron secret salah → 401.
- **Manual PARSIAL** (sebagian disintesis karena tidak ada kredensial Midtrans asli): webhook `PAID→PROCESSING→dispatchFulfillment` (signature+dedup+state machine+panggilan Digiflazz semua ASLI, cuma re-konfirmasi status ke Midtrans disintesis via shim eksternal) — hasil `REFUND_PENDING` karena Digiflazz menolak IP (bukan bug, pola sama Fase 2); invoice polling SN tanpa reload (mekanisme polling ASLI, transisi ke `COMPLETED` dipicu manual di DB karena Digiflazz real tidak pernah sukses akibat IP-whitelist).
- **Sama sekali tidak teruji** (gap pre-go-live, BUKAN blocker Fase 3): panggilan `chargeQris`/`getTransactionStatus` yang benar-benar sukses ke Midtrans sandbox asli. **Harus diulang begitu kredensial Midtrans sandbox asli tersedia**, sebelum go-live.

**Temuan #1 (Important, sudah diperbaiki — commit `c7948cf`)**: `web/src/components/theme-toggle.tsx:10` — `npm run lint` error dari rule `react-hooks/set-state-in-effect` pada `useEffect(() => setMounted(true), [])`. File ini scope Fase 3 (commit `0d7b77b`). Brief mengharapkan lint bersih.

**Temuan #2 (Minor, non-blocking)**: `invoice-status.tsx:140` warning `@next/next/no-img-element` untuk `<img>` render QRIS.

Dev server (`npm run dev`) dipakai untuk verifikasi sudah dihentikan, port 3000 dikonfirmasi tidak listening lagi. Semua toggle data sementara (Product `mobile-legends.isActive`, 1 baris `ProviderSku` test) dikembalikan persis ke semula, dikonfirmasi via query ulang. Semua order/webhook-event test dihapus.

### Final whole-branch review (selesai, sesi 2026-07-25)

Review menyeluruh 17 commit (opus, range `733bb88..c7948cf`) sebelum merge: **Ready to merge: With fixes**. Ditemukan 1 **Critical** (C1) + 8 Important (I1-I8). 2 temuan yang sebelumnya di-park sebagai "minor" di ledger per-task ternyata TERBUKTI SALAH penilaiannya oleh review menyeluruh (naik jadi Important) — dicatat di sini supaya pelajaran ini tidak hilang:

- **C1 (Critical, DIPERBAIKI — commit `b01b17b`)**: kalau `dispatchFulfillment` gagal (network error ke Digiflazz) SETELAH order sudah diklaim `PAID`, order bisa macet permanen di `PROCESSING` — uang masuk, barang tidak terkirim, tanpa retry maupun refund_pending. Fix: guard `dispatchFulfillment` jadi klaim atomik (aman dipanggil ulang), job `recheck-fulfillment` dijadwalkan SEBELUM panggil adapter (bukan cuma setelah hasil "pending"), panggilan adapter dibungkus try/catch, webhook coba dispatch ulang kalau retry menemukan order masih `PAID`.
- **I1 (Important, DIPERBAIKI — commit `b01b17b`)**: ruling ledger Task 5 sebelumnya ("sudah tercegah oleh zod checkoutSchema") **TERBUKTI SALAH** — `z.record` ternyata meloloskan `target: {}` (objek kosong). Fix: validasi eksplisit semua `inputFields` produk terisi di `createCheckoutOrder`, sebelum order dibuat/di-charge.
- **I6 (Important, DIPERBAIKI — commit `b01b17b`)**: `MIDTRANS_SERVER_KEY` kosong bikin verifikasi signature webhook bisa dipalsukan (dihitung dengan key `""`). Ledger Task 6 sebelumnya menilai ini "cuma DX" — TERLALU RINGAN. Fix: webhook fail-fast (500) di awal kalau env var itu tidak di-set.

Re-review scoped setelah fix wave: SEMUA 3 di atas **ADDRESSED**, 81/81 test pass, tidak ada regresi/breakage baru. Laporan lengkap: `.superpowers/sdd/2026-07-25-fase-3-order-midtrans/final-review-fix-report.md`.

**Follow-up PRE-GO-LIVE (non-blocking untuk merge Fase 3, tapi WAJIB sebelum production — catat di rencana Fase 4/7):**
1. Kredensial Midtrans sandbox ASLI belum pernah diuji lewat jaringan nyata (charge sukses + getTransactionStatus asli) — ulangi Task 13 langkah 3.2/3.3 begitu kredensial tersedia.
2. C1 belum 100% tertutup: window sempit antara klaim atomik dan `db.job.create`/order lookup (kalau DB error/crash proses tepat di situ) masih bisa meninggalkan order `PROCESSING` tanpa fulfillment row & tanpa job recheck. Job `recheck-fulfillment` yang `FAILED` permanen (5x attempt gagal) juga tidak eskalasi ke `NEEDS_REVIEW` — order bisa macet diam-diam tanpa terlihat (belum ada halaman admin orders di Fase 3).
3. I2-I5, I7, I8 dari final review (belum diperbaiki, sengaja diparkir): pembayaran sukses tapi order sudah bukan `PENDING_PAYMENT` hilang tanpa jejak; expiry lokal tidak sinkron dengan expiry Midtrans; notifikasi `settlement` yang reconfirm-nya `pending` bisa ke-dedup permanen; `orderNumber` 10rb kombinasi/hari (entropi rendah, `Math.random()`, bisa dienumerasi — SN/voucher berisiko dipanen lewat endpoint status tanpa auth); QRIS string dikirim ke pihak ketiga (`api.qrserver.com`); 2 mapping status uang (`fulfillment.ts`, webhook) belum di-unit-test (masih inline, bukan fungsi pure terpisah).
4. `web/src/lib/midtrans/client.ts` masih fallback `?? ""` untuk server key di `chargeQris`/`getTransactionStatus` (sengaja di luar scope fix I6 — beda risiko, bukan lubang keamanan, cuma bikin auth gagal ke Midtrans).

**Langkah berikutnya**: gunakan `superpowers:finishing-a-development-branch` untuk integrasi branch (`fase-3-order-midtrans-sdd` → `fase-3-order-midtrans`).

### Konsep design system & histori sebelum implementasi (arsip, sudah selesai/diputuskan)

Fase 3 = "Order flow + Midtrans": checkout guest, QRIS, webhook, fulfillment otomatis, invoice + polling. DoD (spec §12): *"Beli 86 Diamonds end-to-end di sandbox: bayar → diamond terkirim → SN tampil."*

**Wajib dikerjakan LEBIH DULU sebelum plan/coding** (spec §15 addendum poin 2): bikin design system dual-theme (light + dark) pakai skill `ui-ux-pro-max` + `frontend-design`, **dipresentasikan ke Wildan untuk approval** sebelum diterapkan ke halaman publik. Referensi rasa: Codashop (terang/playful) untuk light, UniPin (gelap/gaming) untuk dark — dua tema, bukan pilih salah satu.

### Progress konsep design system (sesi 2026-07-25, BELUM DIPRESENTASIKAN ke user)

Riset sudah dijalankan pakai `ui-ux-pro-max` (`--design-system` + `--domain style/color/typography`). Hasil: 2 arah kandidat sudah dirancang, **BELUM dibuat jadi artifact preview visual, belum di-approve user**:

- **Arah A "Ceria & Berani"** (gaya "Vibrant & Block-based" — performa bagus, full light+dark, Tailwind 10/10): Light bg `#F5F3FF`, primary indigo `#4F46E5` (tombol solid pakai `#4338CA` demi kontras), accent oranye `#EA580C`. Dark bg `#0F0F23` (palet "Gaming" dari database), primary ungu neon `#7C3AED`, accent rose `#F43F5E`. Radius besar 20px (chunky/blocky). Font: **Baloo 2** (bold, rounded, playful) untuk heading/display, body pakai system-ui stack.
- **Arah B "Bersih & Tepercaya"** (gaya "Flat Design" — performa Excellent, WCAG AAA, cocok untuk kategori pulsa/PLN/e-money yang butuh kesan tepercaya): Light bg `#F7FBF9`, primary emerald `#059669` (tombol `#047857`), accent amber `#D97706`. Dark bg `#0B1120`, primary emerald muda `#34D399` (tombol `#10B981`, teks tombol gelap `#052e22` demi kontras), accent amber `#F59E0B`. Radius kecil 8px (flat/utility), tanpa shadow. Font: **IBM Plex Sans SemiBold** untuk heading/display, body system-ui.
- Kedua arah pakai token warna semantik sama (success/warning/danger) buat status transaksi (Berhasil/Diproses/Gagal — dipetakan dari fitur test-transaction Fase 2 yang sudah nyata ada).
- Font Baloo 2 (bold, subset karakter yang dipakai saja, ~9.6KB woff2) dan IBM Plex Sans SemiBold (subset, ~13KB woff2) **sudah di-download & di-base64-encode** (dari Google Fonts, subset via param `&text=`) untuk di-embed langsung ke artifact HTML (CSP artifact block font CDN, jadi harus data-URI). **File base64 tersimpan di job tmp dir sesi itu (`$CLAUDE_JOB_DIR/tmp/baloo2_b64.txt` & `ibmplex_b64.txt`) — kemungkinan BESAR SUDAH HILANG di sesi baru** karena itu direktori job sementara. Kalau hilang, ulangi dengan resep ini (cepat, <1 menit):
  ```bash
  # Baloo 2 bold, subset karakter DannShop-relevant:
  curl -s -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/122.0 Safari/537.36" \
    "https://fonts.googleapis.com/css2?family=Baloo+2:wght@700&display=swap&text=DannShopabcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789.,-%20RpDiamondsMobileLegendsFreeFire" \
    -o /tmp/baloo2_subset.css
  # ambil URL woff2 dari file itu (grep src: url(...)), curl -o /tmp/baloo2.woff2 "$URL", lalu base64 -w0

  # IBM Plex Sans SemiBold, subset serupa (tambah kata: PulsaDataEMoneyPLNVoucherTokenTagihanBeliSekarangBerhasilPending)
  curl -s -A "...sama..." \
    "https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@600&display=swap&text=<subset>" \
    -o /tmp/ibmplex_subset.css
  ```
- **Artifact preview SUDAH DIBUAT & DIPRESENTASIKAN** (sesi 2026-07-25, lanjutan): dibangun via subagent implementer (pola sama Fase 1/2) dari spec token di atas + 2 font base64 (masih ada di `C:\Users\ASUS\.claude\jobs\afe12054\tmp\{baloo2,ibmplex}_b64.txt`), lalu direview manual (tag balance, isi konten, kontras) sebelum dipublish. Validasi kontras WCAG dijalankan dulu pakai `ui-ux-pro-max` + perhitungan manual (rumus relative luminance) — 2 kombinasi tombol yang dikhawatirkan (teks putih di tombol ungu dark-mode Arah A, teks gelap di tombol hijau dark-mode Arah B) **lolos AA** (5.70:1 dan 5.83:1). Badge status (Menunggu Pembayaran/Diproses/Berhasil/Gagal) dibuat pola chip tint-bg + teks lebih gelap (bukan warna mentah di atas bg polos) karena warna semantik mentah sebagai teks kecil cuma dapat ~3.2:1 (gagal AA teks normal). Link artifact: `https://claude.ai/code/artifact/d8d7b7e8-277f-4235-95ab-0c1e40293361` (privat, belum di-share user).

**KEPUTUSAN USER (2026-07-25): Arah A "Ceria & Berani" dipilih.** Token final yang dipakai untuk implementasi nanti = spec Arah A persis seperti di atas (light bg `#F5F3FF`/primary `#4F46E5`/btn `#4338CA`/accent `#EA580C`; dark bg `#0F0F23`/primary `#7C3AED`/accent `#F43F5E`; radius 20px; font Baloo 2 700 display + system-ui body). Arah B tidak dipakai — tapi tetap disimpan di histori kalau-kalau user mau revisi balik.

Catatan tambahan sesi ini: user minta semua komunikasi terminal pakai Bahasa Indonesia mulai sekarang (lihat memory `feedback_bahasa_indonesia`).

### Langkah historis (SEMUA sudah selesai, arsip urutan yang dulu dipakai)

1. ~~Bangun artifact HTML preview 2 arah desain~~ ✅ selesai. ~~Tunggu user pilih arah~~ ✅ **Arah A dipilih (2026-07-25).**
2. ~~`superpowers:writing-plans` untuk rencana implementasi Fase 3~~ ✅ selesai — `docs/superpowers/plans/2026-07-25-fase-3-order-midtrans.md`.
3. ~~Eksekusi 13 task via `superpowers:subagent-driven-development`~~ ✅ selesai, termasuk Task 13 verifikasi akhir (lihat bagian "Status Fase 3" di atas).

### Langkah berikutnya yang benar (JANGAN skip urutannya)

1. ~~Fix Temuan #1 dari Task 13 (`theme-toggle.tsx` lint error `react-hooks/set-state-in-effect`)~~ ✅ sudah diperbaiki (commit `c7948cf`).
2. Final whole-branch review (pola sama Fase 1/2: review menyeluruh commit range Fase 3, fix Critical/Important yang ditemukan, re-review).
3. Setelah Fase 3 selesai: **JANGAN asumsikan ada PR** — cek dulu keberadaan PR asli via `gh api` atau GitHub web sebelum menganggap ada proses review tertunda (pelajaran sesi ini: catatan sebelumnya salah kira ada PR Fase 1 padahal tidak pernah benar-benar dibuat).

## Status Fase 2 (mulai 2026-07-24, subagent-driven) — SEMUA 14 TASK SELESAI

Task 1–10 selesai (util key(), webhook signature, Digiflazz adapter, mapTrxStatus, parseCallback, registry adapter, sync harga, kredensial provider terenkripsi UI, CRUD produk/item admin). Task 11 (mapping ProviderSku + margin viewer) selesai commit `44ca067`. Task 12 (halaman transaksi tes Digiflazz) selesai commit `174351d`, review Approved tanpa Critical/Important. Task 13 (seed 4 ProviderConfig + contoh katalog ML & FF) selesai commit `f0becfb`. Task 14 (verifikasi akhir fase) selesai.

### Final whole-branch review + push (2026-07-25)

Review whole-branch (opus, range `9b7a296..4a809c5`, 20 commit): **Ready to merge: Yes**, tanpa Critical. 2 Important ditemukan & langsung di-fix (commit `0851918`):
1. `DigiflazzAdapter.post()` tidak ada timeout & bisa lempar `SyntaxError` mentah kalau respons bukan JSON (mis. halaman HTML dari gateway/rate-limit) — ditambah `AbortSignal.timeout(15s)` + parse JSON aman dengan pesan error jelas.
2. `toggleProductActive` membolehkan aktivasi produk tanpa item, padahal copy halaman admin sudah mengklaim itu tidak mungkin — ditambah guard server-side + tombol "Aktifkan" di-disable di client saat item masih 0.

Re-review commit fix (opus): kedua fix dikonfirmasi benar tanpa regresi. 5 Minor dari review awal **sengaja dibiarkan** (tidak blocking, ikut pola task-task sebelumnya di fase ini): validasi `ProviderKey` cast tidak konsisten antara `actions/catalog.ts` vs `actions/providers.ts`; `CRON_SECRET` compare pakai `!==` bukan `timingSafeEqual`; job runner ada payload rewrite no-op + hasil handler (string ringkasan) tidak pernah disimpan; `ensureRecurringJobs` tidak atomik (potensi job duplikat saat tick bertumpuk) + job `FAILED` permanen langsung dibuat ulang tanpa sinyal kegagalan yang tahan lama; `Number(bigint)` presisi hilang di atas 2^53 (tidak relevan untuk saldo rupiah realistis). Detail lengkap di laporan reviewer (tidak disimpan sebagai file — lihat riwayat sesi 2026-07-25 kalau perlu detail ulang).

**Branch `fase-2-katalog` sudah di-push ke `origin`** (`git push -u origin fase-2-katalog`, sukses). **PENTING:** PR harus dibuat manual via GitHub web (gh CLI tidak terinstall) dengan **base branch `fase-1-fondasi`, BUKAN `main`** — karena `fase-2-katalog` dibuat dari `fase-1-fondasi` (PR Fase 1 belum di-merge; `main` remote & lokal masih di commit paling awal `d33e365`). URL compare: `https://github.com/DannShop/DANNSHOP-E-COMMERCE/compare/fase-1-fondasi...fase-2-katalog?expand=1`.

**Langkah pertama saat lanjut:** cek dulu status kedua PR di GitHub (PR Fase 1 `fase-1-fondasi`→`main` DAN PR Fase 2 `fase-2-katalog`→`fase-1-fondasi`, kalau sudah dibuat user). Kalau Fase 1 sudah merge ke main, sebaiknya PR Fase 2 di-retarget ke `main` di GitHub (tombol "Edit" di halaman PR, ganti base) supaya tidak nge-stack 2 PR. Kalau belum ada progress dari sesi ini, lanjut Fase 3 (cek spec desain buat urutan fase berikutnya) setelah kedua PR beres.

### Catatan sesi 2026-07-25 (Task 14 — verifikasi akhir)

- **Fix tech debt BigInt/tsconfig** (commit `7a79514`): `web/tsconfig.json` target dinaikkan `ES2017` → `ES2020`. Ini akar masalah TS2737 yang dicatat sebagai pre-existing sejak Task 11 (BigInt literal seperti `19750n` butuh ES2020). Setelah fix + hapus `tsconfig.tsbuildinfo` (cache stale menyembunyikan hasil), `npx tsc --noEmit` bersih total.
- Verifikasi penuh dijalankan & PASS semua: `npx vitest run` (49/49 test, 11 file), `npx tsc --noEmit` (bersih), `npm run lint` (bersih), `npm run build` (sukses, 14 route ter-generate termasuk `/api/cron/tick`, `/admin/providers/test-transaction`).
- **DoD end-to-end manual via Playwright MCP + dev server lokal** (kredensial Digiflazz ASLI, sudah tersimpan dari Task 11):
  1. `/admin/providers` → "Cek Saldo" Digiflazz → sukses, health "Sehat", saldo Rp 0 tampil.
  2. "Sync Harga Sekarang" → `PriceSyncLog` result "ok" (0 diupdate/0 hilang — wajar, belum ada mapping ProviderSku di DB dev saat ini).
  3. `/admin/providers/test-transaction` dengan SKU `Aybt69` (sku real dari sesi Task 11) → dapat response signed asli dari Digiflazz: status "Gagal", pesan "IP Anda tidak kami kenali: 103.18.34.217" — **bukan bug**, ini penolakan IP-whitelist di sisi Digiflazz (persis pola yang sudah diverifikasi & diterima di review Task 12). Membuktikan alur create→parse→render bekerja benar end-to-end.
  4. `POST /api/cron/tick`: tanpa header → 401; header salah → 401; `x-cron-secret` benar → `{"ran":0,"failed":0}` (wajar, tidak ada provider aktif/job pending).
- Catatan: saat mencoba GET `/api/admin/provider-price-list?provider=DIGIFLAZZ` (dipakai sku-picker) sempat kena rate-limit asli Digiflazz (rc 83, "Anda telah mencapai limitasi pengecekan pricelist") — dikonfirmasi error dari sisi Digiflazz, bukan bug adapter (adapter melempar pesan jelas, route balas 502 dengan pesan, tidak crash).
- `web/.env` (untracked) ditambah `CRON_SECRET` (belum ada sebelumnya walau sudah ada di `.env.example` sejak Task 8) — perlu untuk uji manual cron tick lokal.
- Dev server (`npm run dev`) yang dipakai untuk verifikasi Playwright sudah dihentikan setelah selesai.

Catatan penting sesi Task 11 (masih relevan):
- DB dev punya kredensial Digiflazz ASLI tersimpan terenkripsi (`ProviderConfig` key=DIGIFLAZZ, isActive=false, health "Sehat"). Jangan expose/log ulang kredensial ini.
- Minor kosmetik non-blocking: `sku-picker.tsx` Select trigger provider menampilkan raw enum value sebelum dropdown pernah dibuka sekali.

## Status eksekusi Fase 1 (subagent-driven)

| Task | Status | Commit |
|---|---|---|
| 1. Scaffold Next.js + Vitest | ✅ selesai + review approved | `441172f` |
| 2. Prisma + skema DB penuh (18 model) | ✅ selesai + review approved (1 fix: .env.example) | `ddcaa22`, `8dcd880` |
| 3. Password util + Zod (TDD) | ✅ selesai + review approved | `f89b170` |
| 4. Seed kategori + admin + **downgrade Prisma 7→6** | ✅ selesai + review approved (1 fix: deprecation warning `prisma.config.ts`) | `bc16ef4`, `5c36510` |
| 5. Auth.js v5 login + register | ✅ selesai + review approved | `22c88bd` |
| 6. Middleware proteksi route | ✅ selesai + review approved (deviasi: `middleware.ts`→`src/proxy.ts`, Next 16 rename, terverifikasi) | `5393d5f` |
| 7. Layout UI publik + admin shell | ✅ selesai + review approved (1 fix: koreksi laporan, bukan kode) | `28c710d` |
| Final whole-branch review | ✅ Ready to merge: Yes (opus) + 1 fix commit + re-review approved | `2a1a846` |

## [HISTORIS — sudah tidak berlaku] Langkah pertama saat lanjut (ditulis akhir sesi Fase 1)

> Catatan 2026-07-25: paragraf di bawah ini SALAH — mengira PR Fase 1 sudah dibuat, padahal setelah dicek ulang tidak pernah ada PR nyata di GitHub sama sekali (`gh api repos/.../pulls` return array kosong). Fase 1 & 2 akhirnya di-merge LANGSUNG ke `main` secara lokal (tanpa PR) di sesi 2026-07-25. Lihat bagian "Status Fase 3" di atas untuk state yang benar. Dibiarkan di sini sebagai jejak historis, jangan diikuti.

**Fase 1 selesai total dan sudah di-PR-kan.** Remote `origin` = `https://github.com/DannShop/DANNSHOP-E-COMMERCE.git` (baru ditambahkan sesi ini, sebelumnya repo ini gak punya remote). `main` dan `fase-1-fondasi` sudah di-push. PR dari `fase-1-fondasi` → `main` sudah dibuat manual oleh user (link waktu itu: `https://github.com/DannShop/DANNSHOP-E-COMMERCE/pull/new/fase-1-fondasi` — cek GitHub buat nomor PR aktualnya). `gh` CLI TIDAK terinstall di environment ini — kalau butuh operasi PR/issue via CLI, install dulu atau lakukan manual via web.

Saat sesi berikutnya mulai, cek dulu status PR itu (merged? ada review comment?) sebelum mulai kerjaan baru. Kalau sudah merge, mulai rencana Fase 2 (spec: `docs/superpowers/specs/2026-07-19-dannshop-topup-platform-design.md`, kemungkinan besar fokus katalog produk/order — cek spec buat urutan fase). Kalau belum merge / ada revisi diminta, lanjutkan di branch `fase-1-fondasi` yang sama.

Backlog Fase 2 (dicatat, bukan blocker Fase 1 — belum ada rencana/task resmi buat ini):
- `authorize()` di `web/src/lib/auth.ts` belum ada automated test (baru manual/Playwright) — plan-level gap.
- `/login` & `/register` di luar route group `(public)` (tidak dapat header/footer) dan masih pakai `<input>`/`<button>` manual, bukan komponen shadcn.
- Race kondisi duplicate-email saat register (`web/src/app/actions/auth.ts`) belum ditangani (`P2002` unhandled → 500 mentah, bukan pesan ramah).

## Catatan penting sesi ini

- **Prisma di-downgrade ke v6** (dari v7) karena Prisma 7 mengubah inisialisasi client (`new PrismaClient()` polos gagal) — keputusan controller, sudah masuk commit `bc16ef4` bersama seed. `prisma.config.ts` dihapus, datasource `url = env("DATABASE_URL")` dikembalikan ke schema. Task 4 review menandai ini scope-creep + downgrade mungkin lebih invasif dari perlu (fix 1-baris di schema mungkin cukup untuk v7) — **diterima as-is** oleh user, tidak diinvestigasi ulang.
- Deprecation warning `package.json#prisma` (Prisma 6.19) sudah **di-fix** di commit `5c36510`: seed config dipindah ke `web/prisma.config.ts` (`migrations.seed`), blok `package.json#prisma` dihapus.
- Kredensial dev di `web/.env` (untracked): admin `admin@dannshop.test` / `Admin-Dev-2026!`, DB `dannshop_next` di MySQL Laragon.
- Final whole-branch review (opus) sudah kelar: verdict **Ready to merge: Yes**. Temuan Important (email gak dinormalisasi) + Minor (zod `.email()` deprecated, `lang="en"` harusnya `"id"`, `@types/bcryptjs` redundant, entry gitignore mati) semua **sudah di-fix** dalam 1 commit `2a1a846`, re-review approved.
- Commit `bc16ef4` masih gabung 2 concern (seed + downgrade Prisma) jadi satu — dicatat sebagai git-hygiene nitpick di final review, TIDAK ada risiko integrasi nyata, diputuskan untuk diabaikan (bukan blocker).
- Seed sudah terbukti idempotent (2x run OK): 5 kategori + 1 admin + wallet.
- Remote `origin` ditambahkan sesi ini (repo sebelumnya belum ada remote sama sekali). `gh` CLI belum terinstall di environment ini.
